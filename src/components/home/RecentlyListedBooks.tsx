/**
 * @deprecated Unused component kept only until safe deletion.
 * Previously listed recently added books; the home page no longer renders
 * it and no file imports it. Safe to remove in a future cleanup.
 */
export default function RecentlyListedBooks() {
  return null;
}
